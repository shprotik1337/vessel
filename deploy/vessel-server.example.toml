# Пример Vessel Server. Скопируй на сервере:
#   cp deploy/vessel-server.example.toml /opt/vessel/server.toml
# Сгенерируй токен:  openssl rand -hex 24   (или pwgen)
# Запуск:           vessel-server /opt/vessel/server.toml
# systemd unit:     deploy/vessel-server.service

[server]
name = "My Vessel Server"
listen = "0.0.0.0:7700"
# HTTPS termination is out of scope for alpha: put caddy/nginx in front if tokens
# must not cross public internet.
tokens = ["REPLACE-ME-long-random-hex"]

# auto — через сервер идёт только то, что иначе не заиграет с клиентского IP
# (googlevideo, расшифрованные файлы). always — весь аудио-трафик через сервер.
# off — relay запрещён (только метаданные/URL, провайдеры с file:// не отдадутся).
relay = "auto"
max_streams = 64
# TTL relay-токенов, сек (аудиосессии живут часами — ставь с запасом)
relay_ttl_secs = 14400
data_dir = "/opt/vessel"
# user_storage / auth — фаза B, здесь ещё нет.

# Дальше — обычные провайдерские секции Vessel: сервер сам хранит credentials
# этого экземпляра в data_dir/secrets.json (или keyring, если есть).
# НЕ переноси в него приватные cookie со своего рабочего компьютера, если не
# хочешь, чтобы сервер ходил в сервисы под твоей учёткой.

soundcloud_enabled = true
yandex_enabled = false
deezer_enabled = false
spotify_enabled = true
youtube_music_enabled = true

# --- YouTube Music на сервере ---------------------------------------------
# YTM на датацентровых IP без логина режется Google: поиск пустой, стримы
# обрезаются до ~1 MiB. Сервер поддерживает три ключа (клиентские credentials
# НЕ переносить — ключи ниже живут только в этом secrets.json):
#
# 1) youtube-cookie — cookie залогиненного YouTube-аккаунта (SID, HSID, SSID,
#    SAPISID, LOGIN_INFO и др. одной строкой). Снимает бот-чек поиска и даёт
#    полные форматы WEB_REMIX. Самый простой и надёжный путь.
#    Заполни через:  cargo run -p vessel-server -- ...  или впиши в secrets.json:
#      {"values": {"youtube-cookie": "SID=...; HSID=...; SSID=...; SAPISID=...;"}}
#
# 2) youtube-oauth-refresh — refresh_token TV device-flow (альтернатива cookie).
#
# 3) youtube_potoken_provider — URL bgutil-совместимого HTTP-провайдера PO
#    token (bgutil-ytdlp-pot-provider, /get_pot). POT обязателен: без него
#    googlevideo отдаёт только первый MiB. Провайдер держи на той же машине
#    (127.0.0.1:4416) — токен привязан к IP выхода.
youtube_potoken_provider = "http://127.0.0.1:4416"

# после старта:
# curl -H "Authorization: Bearer <токен>" http://127.0.0.1:7700/api/v1/capabilities
